# Local dev: Vite proxy target for /api requests
VITE_DEV_API_PROXY_TARGET=http://localhost:5000

# Production (or separate frontend/backend deployments):
# set this to your deployed backend base URL
# VITE_API_BASE_URL=https://your-backend-domain.com
